import { WhitespacesDirective } from './Nowhitespaces.directive';

describe('WhitespacesDirective', () => {
  it('should create an instance', () => {
    const directive = new WhitespacesDirective();
    expect(directive).toBeTruthy();
  });
});
